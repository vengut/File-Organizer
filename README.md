# File-Organizer
Goes through video files in folder. Move the file to folder based on highest probability name match.
Writes the Source and Destination address to log.

implements [fuzzywuzzy](https://github.com/seatgeek/fuzzywuzzy).
